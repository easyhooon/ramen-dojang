# 2026-07-06 Worklog

## 한 줄 요약

Stitch 시안 zip의 `shoyu_amber` 방향을 기준으로 웹앱의 1차 화면 톤을 파란 계열에서 라멘/쇼유 앰버 계열로 전환했다.

## Frontend

- TDS provider와 Granite 앱 브랜드 primary color를 `#b45309`로 맞췄다.
- 홈 상단 요약 영역을 큰 히어로보다 통계 카드 중심으로 보이도록 CSS 구성을 조정했다.
- 카드, 패널, 목록, 선택 카드, 폼, 별점, 기본 버튼의 색상과 반경을 Shoyu Amber 토큰에 맞춰 정리했다.
- 라멘집 카드 태그 배지를 파란색에서 노란 계열 TDS weak badge로 바꿨다.
- 기본 라멘 placeholder SVG에 남아 있던 파란 그릇 색을 갈색 계열로 바꿨다.
- Stitch 시안 구조에 맞춰 하단 탭 내비게이션, 홈 프로필 카드, 큰 방문 기록 CTA, 추천 카드, 라멘집 목록 필터 칩을 추가했다.
- 방문 추가 화면은 시안의 사진 영역을 실제 업로드 기능 대신 선택한 라멘집 썸네일 미리보기로 반영했다.
- 라멘집 상세, 방문 상세, About 화면도 현재 데이터로 표현 가능한 범위에서 히어로/정보 카드/요약 카드 구조를 적용했다.

## 문서화 / 디자인

- 6월 30일 남은 일 중 Stitch 결과를 코드 적용 대상으로 검토했다.
- Figma MCP로 옮기는 단계는 지금 비용이 더 크다고 판단하고, Stitch zip의 `shoyu_amber` 토큰과 화면 구조를 바로 코드에 반영하는 흐름으로 정했다.
- 화면별 IA/카피는 현재 MVP 기능과 맞는 부분을 우선 반영하고, 프로필/공유/사진 업로드처럼 아직 없는 기능은 껍데기 UI로 만들지 않았다.

## 검증한 것

- `node_modules/.pnpm/typescript@5.9.3/node_modules/typescript/bin/tsc -p apps/web/tsconfig.json --noEmit` 통과.
- `apps/web`에서 Vite 직접 빌드 통과.
- `http://localhost:5173/` HTTP 200 확인.

## 남은 일

- Stitch 결과를 [Frontend Design Direction](../05-frontend-design-direction.md)의 Stitch Archive에 보관한다.
- 실제 브라우저/앱인토스 샌드박스에서 홈, 라멘집 목록, 방문 추가, 라멘집 상세, 방문 상세, About 화면의 모바일 시각 검증이 필요하다.
- 사진 업로드, 공유하기, 영업시간 같은 시안 요소는 실제 요구사항이 확정될 때 별도 기능으로 설계한다.
- `pnpm typecheck`는 pnpm 11 approve-builds 정책 때문에 실패했으므로 설치/검증 절차 정리가 필요하다.

## 관련 커밋

- `4f82b7b` feat: 라멘 색상 기반 화면 톤 적용
- `392d646` docs: Stitch 디자인 기준과 작업 기록 정리
