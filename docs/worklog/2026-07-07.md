# 2026-07-07 Worklog

## 한 줄 요약

배포 전 개발 단계에 맞춰 `shop_candidates` schema를 V1 초기 migration에 합치고, 로컬 PostgreSQL 17/PostGIS DB를 reset한 뒤 Naver 후보 sync/promote로 214건 라멘집 seed를 확보했다.

## Backend

- `V2__create_shop_candidates.sql`을 제거하고 `shop_candidates` 테이블과 관련 index를 `V1__create_initial_schema.sql`에 합쳤다.
- 후보 데이터를 검수하기 위한 admin API 초안을 추가했다.
  - `GET /admin/shop-candidates`
  - `POST /admin/shop-candidates/{candidateId}/promote`
- 후보 승격 시 `shops`에 라멘집을 만들고 `shop_candidates.promoted_shop_id`, `status=promoted`, `reviewed_at`을 갱신하도록 했다.
- 방문 기록이 없는 라멘집의 `averageRating`이 `0.0`으로 내려가던 `ResultSet.wasNull()` 사용 버그를 수정했다.
- 네이버 지역 검색 API 키를 로컬 `.env`에 넣은 뒤 마포/홍대권 라멘집 후보를 수집하고, `일본식라면` 카테고리 후보 23건을 `shops`로 승격했다.
- 이후 전국 주요 도시/상권 쿼리로 후보를 확장했고, `일본식라면` 카테고리 후보를 추가 승격해 `shops`를 214건까지 채웠다.
- 누락된 `멘야준`을 Naver 검색 API로 보강해 홍대점, 소바하우스멘야준 연남점 2건을 추가 승격했다.
- Swagger의 라멘집 태그 설명을 스타일 필터가 아니라 표시/등록용 분류 태그 기준으로 맞췄다.
- Spring Boot 내장 Tomcat이 WAS 역할을 하며, 현재 프로젝트에는 WAR 배포가 필요 없다는 내용을 `LESSONS.md`에 정리했다.

## DB / Local Infra

- 로컬 DB는 배포 전까지 migration을 계속 쌓기보다 reset 후 V1을 다시 적용하는 흐름으로 정리했다.
- Homebrew `postgresql@16`에는 PostGIS extension 파일이 없어 `postgresql@17` 서비스로 전환했다.
- 앱 계정 `ramen`은 `CREATE EXTENSION postgis` 권한이 없으므로, 로컬 DB 생성 직후 superuser로 PostGIS extension을 먼저 설치하고 서버를 띄웠다.
- reset 후 Flyway가 `Successfully validated 1 migration`, `Successfully applied 1 migration` 상태로 V1만 적용하는 것을 확인했다.

## Deployment / Vercel

- Vercel CLI를 설치하고 `easyhooon` 계정으로 로그인했다.
- GitHub repository `easyhooon/ramen-dojang`와 Vercel 프로젝트 연결을 시도했고, GitHub App 권한은 브라우저에서 직접 연결했다.
- Vercel 프로젝트 `ramen-dojang-web`을 생성하고 `apps/web`을 Root Directory로 설정했다.
- monorepo 기준으로 install/build/output 설정을 맞췄다.
  - Install Command: `cd ../.. && pnpm install --frozen-lockfile`
  - Build Command: `cd ../.. && pnpm build`
  - Output Directory: `dist`
- preview 환경변수 `VITE_API_BASE_URL=http://192.168.10.104:8080`을 CLI로 등록했다.
- 로컬 Vercel 연결 파일이 커밋되지 않도록 `.vercel/`을 `.gitignore`에 추가했다.
- Vercel preview deployment의 의미와 로컬 API를 바라볼 때의 mixed content/CORS 한계를 `LESSONS.md`에 정리했다.

## 검증한 것

- `server/api ./gradlew compileKotlin` 통과
- `packages/api-client`와 `apps/web`의 `tsc --noEmit` 직접 실행 통과
- `server/api ./gradlew test` 통과
- `curl http://localhost:8080/health` 응답 `{"status":"ok"}` 확인
- `POST /shops`로 테스트 라멘집 1건 등록 확인
- `GET /shops`와 `GET /shops?name=라멘`에서 등록 데이터와 기본 썸네일 `/assets/default-ramen.svg` 확인
- `psql`로 `shops` 1건, `tags/shop_tags`의 `라멘,쇼유` 저장 확인
- `POST /admin/shop-candidates/sync`는 Naver credentials 미설정 상태에서 `NAVER_SEARCH_NOT_CONFIGURED` 503 반환 확인
- Naver credentials 설정 후 `홍대/합정/망원/연남/상수/신촌/마포 라멘` 후보 sync 확인
- `일본식라면` 후보 23건 승격 후 `shops=23`, `shop_candidates promoted=23`, `pending=3` 확인
- 전국 주요 도시/상권 Naver sync 중 속도 제한 429를 확인했고, 이미 저장된 후보를 기준으로 `shops=166`, `shop_candidates promoted=166`, `pending=53`까지 승격 확인
- `멘야준` Naver sync 2건과 승격 후 `GET /shops?name=멘야준`에서 홍대점, 소바하우스멘야준 연남점 반환 확인
- 이후 제주/순천/여수/목포/서귀포/군산/익산/구미/안동/속초/동탄/평택/파주/양산 등 추가 query를 단일 sync로 보강했고, 이번 보강 구간에서는 429 없이 `shops=214`, `shop_candidates promoted=214`, `pending=75`까지 확인
- 전체 `shops` JSON export를 `/private/tmp/ramen-dojang-shops-2026-07-07.json`에 생성
- 다른 환경에서도 재사용할 수 있도록 id를 제외한 214건 seed JSON을 `server/api/src/main/resources/seed/shops.seed.json`에 저장
- 클라우드 DB가 없는 현재 단계에서는 DB 공유 대신 seed JSON export를 기준 데이터 이동 방식으로 쓰기로 정리
- seed JSON은 GitHub 업로드 가능한 공개 후보 데이터지만, 운영 확정 데이터가 아니라 초기 seed 후보/검수 전 데이터로 취급
- 실제 seed의 태그가 대부분 `라멘`, `일본식라면`이라 목록 필터로 의미가 없어 `GET /shops`의 `tag` query와 웹의 태그 필터 입력을 제거
- Expo/WebView LAN 확인을 위해 개발 CORS 허용 origin에 `http://192.168.*:*`를 추가
- 방문 추가 화면을 라멘집 검색/선택 단계와 리뷰 작성 단계로 분리해, 긴 검색 리스트 아래에 리뷰 폼이 숨어 있지 않도록 수정
- MVP 사용자 의견 수집은 메일 대신 Google Form 링크로 모으기로 결정했다.
- `http://localhost:5173/` 홈 화면이 서버 catalog를 받아 다음 후보를 렌더링하는 것을 확인
- `/shops` 목록 화면이 서버 catalog를 카드로 렌더링하고, 이름 검색이 API 데이터 기준으로 줄어드는 것을 확인
- `/shops/{shopId}` 상세 화면이 `GET /shops/{shopId}` 데이터로 이름과 주소를 렌더링하는 것을 확인
- `/visits/new` 방문 추가 화면에서 `멘야준` 검색 시 홍대점, 소바하우스멘야준 연남점 2건만 노출되는 것을 확인
- `http://192.168.200.33:5173/` Vite dev server 응답과 `Origin: http://192.168.200.33:5173` 기준 API CORS 응답 확인
- `/visits/new`에서 선택 전에는 리뷰 폼이 없고, 라멘집 선택 후에는 검색 리스트가 사라지고 리뷰 작성 폼만 렌더링되는 것 확인
- Expo Go 실기기 확인 시 폰과 개발 컴퓨터가 같은 Wi-Fi/LAN에 있어야 `192.168.x.x` 주소가 열린다는 주의사항을 모바일 문서에 추가
- `vercel project inspect ramen-dojang-web --scope easyhooons-projects`로 Root Directory, Vite preset, install/build/output 설정 확인
- `vercel env list preview --scope easyhooons-projects`로 preview 환경의 `VITE_API_BASE_URL` 등록 확인

## 남은 일

- pending 후보 75건을 수동 검수한다.
- Naver sync는 짧은 시간에 많이 호출하면 429 속도 제한이 걸리므로, 대량 seed가 필요하면 딜레이/재시도 또는 Kakao Local API를 추가한다.
- Naver 응답 주소 중 `전남광주통합특별시`처럼 정규화가 필요한 값이 있어 주소 정규화 규칙을 검토한다.
- seed JSON import script 또는 admin endpoint를 만들면 새 로컬 DB와 다른 컴퓨터에서도 같은 214건 catalog를 쉽게 복원할 수 있다.
- Swagger UI에서 shops CRUD와 후보 sync/promote 흐름을 수동 smoke test한다.

## 관련 커밋

- `e2b685b` feat: 라멘집 후보 승격 API 정리
